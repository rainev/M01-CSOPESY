#pragma once
#include <array>
#include <list>
#include <vector>
#include <queue>
#include <unordered_map>
#include <utility>

namespace csopesy {
  using std::array;
  using std::vector;
  using std::queue;
  using std::unordered_map;
  using std::pair;
  template<typename Type> using List = std::list<Type>;
  using std::make_pair;
}
